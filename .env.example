# PostgreSQL connection used only on the server. Never prefix this with NEXT_PUBLIC_.
DATABASE_URL=postgresql://postgres:password@localhost:5432/lumaflow

# Fail instead of falling back to JSON when PostgreSQL is unavailable.
POSTGRES_REQUIRED=false

# Maximum number of pooled PostgreSQL connections used by the Next.js server.
PG_POOL_MAX=10

# Persistent API writes are off by default. In production, prefer real OIDC/session auth.
DEMO_WRITES_ENABLED=false

# Optional server-to-server credential for write endpoints. Use a long random value.
API_WRITE_TOKEN=

# These values configure only the "configured" model-selector profile.
# The built-in local-qwen3-8b profile needs no env file; use npm run local:setup.
# Backend: vllm, ollama (Qwen3 no-thinking), openai-compatible, or vercel-ai-gateway.
# Never use NEXT_PUBLIC_ for model credentials. Public remote endpoints require LLM_API_KEY.
LLM_BACKEND=vllm
LLM_BASE_URL=http://127.0.0.1:8000/v1
LLM_API_KEY=local
LLM_MODEL=lumaflow-qwen
LLM_TIMEOUT_MS=290000

# Optional server-owned catalog metadata and selection. For a single custom
# model, set both profile values to configured.
LLM_DEFAULT_PROFILE=local-qwen3-8b
LLM_VISIBLE_PROFILES=local-qwen3-8b,local-qwen3-14b,configured
LLM_DISPLAY_NAME=
LLM_DESCRIPTION=
LLM_FAMILY=custom
LLM_PARAMETER_SIZE_B=
LLM_CONTEXT_TOKENS=
LLM_SUPPORTED_MODES=light,medium,ultra

# Per-generation output ceiling, including thinking tokens: integer 256–32768.
# Choose a value supported by the model's context window and available memory.
LLM_MAX_OUTPUT_TOKENS=4096
# In openai-compatible mode, FAST/NORMAL/DEEP select output budgets only.
# Thinking and tool-parser support must be configured in that runtime itself.

# Optional server-to-server token for /api/v1/assistant/chat. Browser calls use same-origin protection.
ASSISTANT_API_TOKEN=

# Permit the deployed knowledge UI to use same-origin knowledge endpoints.
# Direct requests without same-origin browser headers still require ASSISTANT_API_TOKEN.
REMOTE_KNOWLEDGE_ENABLED=false

# Private Vercel Blob store for account-isolated cloud uploads. Vercel adds this
# automatically when a private Blob store is connected to the project.
BLOB_READ_WRITE_TOKEN=

# Cloud collaborative calendar: signed-in accounts share individual events by
# invited email address. Local browser sessions connect to the shared Vercel
# calendar with an eight-hour account token; no cloud storage secret is needed
# on each user's computer. Configure these server-only values in Vercel.
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_SECRET=
APP_BASE_URL=
WORK_PAIRING_SIGNING_KEY=
# Optional server-side comma-separated allowlist if local installations use a
# different loopback port. Keep this narrow and configure it only on Vercel.
CALENDAR_LOCAL_ORIGINS=http://127.0.0.1:3000,http://localhost:3000

# Optional public URL of the shared calendar host for local installations.
# All collaborators must point to the same deployed LumaFlow site.
NEXT_PUBLIC_CALENDAR_URL=https://lumaflow-sales-hub.vercel.app

# Optional TypeSafe Jev category suggestions. Keep server-side; never use NEXT_PUBLIC_.
TYPESAFE_API_KEY=
